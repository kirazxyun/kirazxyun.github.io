{
  "name": "kirazxyun.github.io",
  "version": "1.0.0",
  "description": "blog",
  "main": "index.js",
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "npm run build && git add . && git commit -m 'deploy' && git push"
  },
  "author": "kira",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^0.8.4"
  }
}
